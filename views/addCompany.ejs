<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Firma hinzufügen</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <h1>Neue Firma hinzufügen</h1>
    </header>
    <main>
        <% if (error) { %>
            <p style="color: red;"><%= error %></p> <!-- Zeigt Fehlermeldungen aus dem Backend an -->
        <% } %>

        <form action="/companies/add" method="POST">
            <label for="name">Name der Firma:</label>
            <input type="text" id="name" name="name" required>
        
            <label for="department">Abteilung:</label>
            <input type="text" id="department" name="department" required>
        
            <label for="password">Passwort (Klartext):</label>
            <input type="password" id="password" name="password" required>
        
            <label for="startDate">Startdatum:</label>
            <input type="date" id="startDate" name="startDate" required>
        
            <label for="endDate">Ablaufdatum:</label>
            <input type="date" id="endDate" name="endDate" required>
        
            <button type="submit">Firma speichern</button>
        </form>
    </main>
</body>
</html>